<nav>
<ul class="Navbar">   
<li class="navText">
    {{#if adminLogin}}
    <span>Hi Tutor</span>
    {{else}}
    <span>Hi {{student.Name}}</span>
    <span class="notification1"><a href="/assignments">New Assignment Added </a></span>
    {{/if}}
</li>
<li class="NavbarLogout">
    {{#if adminLogin}}
    
   <a href="/admin/logout"><button>Logout</button></a>
    {{else}}
    <a href="/logout"><button>Logout</button></a>
    {{/if}}
</li>
</ul>
</nav>


    <script>
      var socket = io.connect();
       socket.on('connect', () => {
        });
        socket.on('AssignmentAdded',(data)=>{
            $('.notification1').addClass('notification').removeClass('notification1')
        })
    </script>